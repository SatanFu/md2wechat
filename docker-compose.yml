services:
  md2wechat:
    build: .
    ports:
      - "${HOST_PORT:-3090}:3090"
    env_file:
      - .env
    volumes:
      - ./config:/app/config
      - ./themes:/app/themes
      - ./data:/app/data
    restart: unless-stopped
